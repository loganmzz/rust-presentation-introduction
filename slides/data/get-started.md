## Premier code

* IDE (RLS, racer, rustfmt, ....)
* VS Code ou slide
* exemples _(avec annotations de type)_
    * `while let mut` (+ `if let mut` ?)
    * `match` + `enum`
    * Option / Result
* cargo build
